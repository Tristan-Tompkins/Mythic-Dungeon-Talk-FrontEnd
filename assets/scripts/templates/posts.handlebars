{{#each posts as |post|}}

  <div class="post my-2 border post-item rounded p-2">
    <h5 class="font-weight-bold m-2">{{post.user_info}} posted: </h5>
    <h5 data-id="{{post.id}}" class="font-italic m-2 post-text">"{{post.text}}"</h5>
  </div>

{{/each}}
